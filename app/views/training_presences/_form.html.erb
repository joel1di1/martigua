<%= simple_form_for(@training_presence) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :training %>
    <%= f.association :user %>
    <%= f.input :status %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
